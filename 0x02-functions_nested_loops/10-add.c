#include "main.h"
/**
 * add - function to add two integers
 * @a - first integer
 * @b - second integer
 * Return: returns the result of the addition
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
