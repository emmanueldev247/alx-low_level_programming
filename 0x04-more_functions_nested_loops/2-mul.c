#include "main.h"
/**
 * mul - function to multiply 2 ingtegers
 * @a: Argument holding first integer
 * @b: Argucment holding second integer
 * Return: returns the product of the two integers
 */
int mul(int a, int b)
{
	return (a * b);
}
